name: Build and Deploy Order Service

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Azure Container Registry
      uses: docker/login-action@v2
      with:
        registry: dealanregistry2026yazid.azurecr.io
        username: dealanregistry2026yazid
        password: 77ZkDLCa97j6AK9baB2scCDwXw2vAALlP7w2KQM37JPmX2qSroA1JQQJ99CFAC3pKaREqg7NAAACAZCRHPs2

    - name: Build and push Order Service
      uses: docker/build-push-action@v4
      with:
        context: ./order-service
        file: ./order-service/Dockerfile
        push: true
        tags: dealanregistry2026yazid.azurecr.io/dealan-order-service:latest
